<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="240dp"
    android:layout_height="80dp"
    android:background="@drawable/bottom_border">

    <TextView
        android:id="@+id/firstCharacter"
        android:layout_width="56dp"
        android:layout_height="56dp"
        android:layout_marginStart="4dp"
        android:text="账"
        android:gravity="center"
        android:textAlignment="center"
        android:textSize="32sp"
        android:textStyle="bold"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <TextView
        android:id="@+id/accountNickName"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginStart="8dp"
        android:layout_marginTop="4dp"
        android:text="账户名称"
        android:textSize="16sp"
        app:layout_constraintVertical_chainStyle="packed"
        app:layout_constraintStart_toEndOf="@id/firstCharacter"
        app:layout_constraintTop_toTopOf="@id/firstCharacter"
        app:layout_constraintBottom_toTopOf="@id/accountEmail"/>

    <TextView
        android:id="@+id/accountEmail"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="1dp"
        android:text="邮箱"
        android:textSize="12sp"
        android:textColor="@color/liteWhite"
        app:layout_constraintStart_toStartOf="@id/accountNickName"
        app:layout_constraintTop_toBottomOf="@id/accountNickName"
        app:layout_constraintBottom_toBottomOf="@id/firstCharacter"
        />

    <ImageView
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:layout_marginEnd="2dp"
        android:src="@drawable/chevron_right"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:tint="@color/liteWhite" />
</androidx.constraintlayout.widget.ConstraintLayout>