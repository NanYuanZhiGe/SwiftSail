<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.SwiftSail" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.SwiftSail.Fullscreen" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- 关键：让内容延伸到系统窗口（状态栏、导航栏） -->
        <item name="android:windowTranslucentStatus">true</item>
        <!-- 可选：如果底部也有导航栏，也透过去 -->
        <!-- <item name="android:windowTranslucentNavigation">true</item> -->
        <item name="android:windowBackground">@android:color/white</item>
    </style>

    <style name="Theme.SwiftSail" parent="Base.Theme.SwiftSail" />
</resources>